from setuptools import setup, find_packages

setup(
    name='video_object_detection_metrics',
    version='0.1',
    packages=['video_object_detection_metrics', 'video_metrics_test']
)

setup(
    name='project_utils',  # Your project name
    version='0.1',
    packages=find_packages(),  # Automatically find all packages
)


from setuptools import setup, find_packages

setup(
    name="mmdet",
    version="0.1.0",
    packages=find_packages(),
)


